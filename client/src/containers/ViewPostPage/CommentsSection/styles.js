export default (theme) => ({
  root: {
    marginTop: theme.spacing(1),
    marginBottom: theme.spacing(2),
  },
  commentField: {
    width: '30%',
  },
  submitButton: {
    marginTop: theme.spacing(1),
  },
});
